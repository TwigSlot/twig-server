apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-twigslot.fullname" . }}-test-connection"
  labels:
    {{- include "helm-twigslot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-twigslot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
